import { AsFormGroupArrayPipe } from './as-form-group-array.pipe';

describe('AsFormGroupArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new AsFormGroupArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
